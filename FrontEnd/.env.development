# FileSphere - Development Environment Configuration
# All requests are sent to the single API Gateway entry point (port 7000)

# Backend service base URLs
VITE_API_BASE_URL=http://localhost:7000

# App Settings
VITE_ENV=development
VITE_DEBUG=true
